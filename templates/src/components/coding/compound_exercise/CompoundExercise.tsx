import React from "react";
import style from "./CompoundExercise.module.scss";
import classNames from "classnames/bind";

const cls = classNames.bind(style);

const CompoundExercise = () => {
    return <div className={cls("compound_exercise")}>Updating</div>;
};

export default CompoundExercise;
